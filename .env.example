# Configuration des clés API pour les LLMs
# Copier ce fichier en .env et remplir avec vos vraies clés

# OpenAI API Key (pour GPT-4o-mini)
OPENAI_API_KEY=sk-your-openai-api-key-here

# Anthropic API Key (pour Claude 3 Haiku)
ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key-here

# Configuration du LLM par défaut
# Options: "gpt-4o-mini", "claude-3-haiku-20240307"
DEFAULT_LLM_MODEL=gpt-4o-mini

# Température pour la classification (0.0 = déterministe)
LLM_TEMPERATURE=0.0

# Configuration CORS
# Origines autorisées pour les requêtes CORS (format JSON).
# La valeur doit être une liste JSON valide.
# Exemple pour ajouter l'accès depuis une adresse IP locale :
# CORS_ALLOWED_ORIGINS='["http://localhost:3000", "http://localhost:3001", "http://192.168.1.95:3000"]'
CORS_ALLOWED_ORIGINS='["http://localhost:3000", "http://localhost:3001"]'

# Configuration JWT pour l'authentification
# IMPORTANT: Changez SECRET_KEY en production avec une clé aléatoire sécurisée
# Générez une clé avec: openssl rand -hex 32
SECRET_KEY=your-secret-key-change-this-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
